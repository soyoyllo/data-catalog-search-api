# 기본 OpenMetadata URL (watchfiles가 감시함)
OPENMETADATA_BASE_URL=https://localhost:8585/my-data

# 메타데이터 JSON 파일 경로 (컨테이너 작업 디렉터리 기준)
METADATA_FILE_PATH=metadata/enriched_metadata_clustered.json

# FAISS 인덱스 기본 저장 디렉터리 (컨테이너 작업 디렉터리 기준)
FAISS_INDEX_DIR=faiss_indices
